PKG_CPPFLAGS= -I../windows/nlopt-2.4.2/include
PKG_LIBS= -L../windows/nlopt-2.4.2/lib${R_ARCH} -lnlopt_cxx

all: clean winlibs

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"

clean:
	rm -f $(SHLIB) $(OBJECTS)
