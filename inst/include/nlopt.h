// For backwards compatibility only.
//
// Developers should only include `nloptrAPI.h`.
// If they want to include `nlopt.h` and not `nloptrAPI.h`,
// they are encouraged to start using:
// LinkingTo: nloptr (>= 2.3.0) and `#include <nloptr/nlopt.h>`
#include "nloptr/nlopt.h"
